{
  "name": "combine-functions-into-class-tutorial",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build --target es2022",
    "preview": "vite preview --host 127.0.0.1",
    "serve:dist": "node scripts/serve-dist.mjs",
    "test": "vitest run",
    "lint": "biome check . && stylelint \"src/styles/*.css\"",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@biomejs/biome": "^2.5.0",
    "@playwright/test": "^1.61.0",
    "c8": "^10.1.3",
    "jscpd": "^4.2.5",
    "madge": "^8.0.0",
    "stylelint": "^16.26.1",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
